<UserControl x:Class="Hs.Hypermint.IntroVideos.Views.AviSynthOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             >
    <StackPanel>

        <DockPanel Margin="5" MaxHeight="30">
            <Label  Content="Pos X"/>
            <metro:NumericUpDown Minimum="0" Maximum="2000" ToolTip="X Position of image"
                                 IsEnabled="{Binding Overlay}" Value="{Binding WheelPosX}"/>
            <Label Content="Pos Y"></Label>
            <metro:NumericUpDown Minimum="0" Maximum="2000" ToolTip="Y Position of image"
                                 IsEnabled="{Binding Overlay}" Value="{Binding WheelPosY}"/>

            <Label Content="Width"></Label>
            <metro:NumericUpDown Minimum="0" ToolTip="Image width"
                                 IsEnabled="{Binding ResizeOverlay}" Maximum="2000" Value="{Binding ResizeWidth}"/>
            <Label Content="Height"></Label>
            <metro:NumericUpDown Minimum="0" ToolTip="Image height"
                                 IsEnabled="{Binding ResizeOverlay}" Maximum="2000" Value="{Binding ResizeHeight}"/>
        </DockPanel>
        
    </StackPanel>
    
</UserControl>
