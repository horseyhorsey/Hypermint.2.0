<Grid x:Class="Hs.Hypermint.SidebarSystems.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:prism="http://prismlibrary.com/"
            prism:ViewModelLocator.AutoWireViewModel="True"  
            xmlns:views="clr-namespace:Hs.Hypermint.SidebarSystems.Views"
            VerticalAlignment="Top"
             mc:Ignorable="d">

    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"></RowDefinition>
        <RowDefinition Height="400*"></RowDefinition>
        <RowDefinition Height="75"></RowDefinition>
    </Grid.RowDefinitions>

    <views:MainMenuView Grid.Row="0"/>
    <GroupBox Header="Systems" Grid.Row="1" MinHeight="500" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <views:SystemsView Grid.Row="1"/>
    </GroupBox>

    <GroupBox Header="System Filter" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
        <TextBox Text="{Binding SystemTextFilter,UpdateSourceTrigger=PropertyChanged}"
                 metro:TextBoxHelper.ClearTextButton="True" 
                     metro:TextBoxHelper.Watermark="System Name"></TextBox>
    </GroupBox>
</Grid>



