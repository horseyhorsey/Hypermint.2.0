<UserControl x:Class="Hs.Hypermint.SidebarSystems.Views.SystemsFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignWidth="150">
    <Grid>
        <GroupBox Header="System Filter" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
            <TextBox Text="{Binding SystemTextFilter, UpdateSourceTrigger=PropertyChanged}"
                 metro:TextBoxHelper.ClearTextButton="True" 
                     metro:TextBoxHelper.Watermark="System Name"></TextBox>
        </GroupBox>
    </Grid>
</UserControl>
