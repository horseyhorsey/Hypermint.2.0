<UserControl x:Class="Hs.Hypermint.SidebarSystems.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <GroupBox Header="{Binding MenusHeader}">
        <ComboBox ItemsSource="{Binding MainMenuDatabases}" DisplayMemberPath="Name"
                          ToolTip="{Binding Path}"
                          SelectedItem="{Binding SelectedMainMenu}"/>
        </GroupBox>
    </Grid>
</UserControl>
