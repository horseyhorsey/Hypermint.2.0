<UserControl x:Class="Hs.Hypermint.HyperspinFile.Views.HyperspinFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"          
             xmlns:controls="clr-namespace:Hs.Hypermint.HyperspinFile.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="85"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="{Binding MediaTypeName}">
            <controls:HsControlsView />
        </GroupBox>

        <GroupBox Header="{Binding GroupBoxHeader}" Grid.Row="1">
            <controls:FilesList/>
        </GroupBox>
        

    </Grid>
</UserControl>
