<UserControl x:Class="Hs.Hypermint.IntroVideos.Views.ScanAddVideosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"           
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid>
        <GroupBox Header="Available videos" Grid.Row="1">
            <!--List for videos-->
            <DataGrid BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="3"
                  ItemsSource="{Binding GameVideos}" VerticalAlignment="Stretch">
                <DataGrid.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBlock Text="{Binding FileName}" />
                            <TextBlock Text="{Binding Format}"/>
                            <TextBlock Text="{Binding FrameRate}"/>
                        </DockPanel>
                    </DataTemplate>
                </DataGrid.ItemTemplate>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
