<UserControl x:Class="Hs.Hypermint.SidebarSystems.SystemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hs.Hypermint.SidebarSystems"
             mc:Ignorable="d" 
                                  xmlns:prism="http://prismlibrary.com/"
            prism:ViewModelLocator.AutoWireViewModel="True"   
             d:DesignHeight="300" d:DesignWidth="300">
    
    <ListBox Grid.Row="2" Height="760"                
                   BorderThickness="0" HorizontalAlignment="Left" ItemsSource="{Binding SystemItems}" 
                       SelectionMode="Single">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <DockPanel>
                    <Image Source="{Binding SysIcon}"></Image>
                    <TextBlock Text="{Binding Name}"></TextBlock>
                </DockPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
    
</UserControl>
